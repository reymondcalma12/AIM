@model IEnumerable<AIM.Models.AppDependency>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
          
            <th>
                RPortOsla
            </th>
            <th>
                Application
            </th>
          
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
           
                <td>
                    @Html.DisplayFor(modelItem => item.RPortOsla.RPortOslaCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Application.AppName)
                </td>
             
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = $"{item.RPortOsla.RPortOslaCode}-{item.Application.AppCode}" }) |
                    @Html.ActionLink("Details", "Details", new { id = $"{item.RPortOsla.RPortOslaCode}-{item.Application.AppCode}" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = $"{item.RPortOsla.RPortOslaCode}-{item.Application.AppCode}" })
                </td>
            </tr>
        }
    </tbody>
</table>
